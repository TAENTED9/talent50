import { SectionHeading } from './ui/SectionHeading';

export default function Engine() { return <section id="engine" className="bg-ink py-[clamp(84px,13vh,170px)]"><div className="mx-auto max-w-content px-5 md:px-[5vw] lg:px-[72px]"><SectionHeading eyebrow="04 — The advantage" title="Everything we do compounds into one advantage." titleClassName="max-w-[20ch]" /><div className="mb-[clamp(48px,6vh,76px)] flex flex-wrap items-baseline gap-x-5 gap-y-3 border-y border-text-light/15 py-[clamp(28px,4vh,44px)] font-display text-[clamp(20px,2.3vw,32px)]"><span>Teams</span><span className="text-text-light/40">+</span><span>Media</span><span className="text-text-light/40">+</span><span>Tech</span><span className="text-text-light/40">=</span><span className="font-bold text-accent">The Talent 50 advantage</span></div><blockquote className="max-w-[26ch] font-display text-[clamp(24px,3vw,44px)] font-medium leading-[1.2] tracking-[-0.015em]">We own the teams, the channels that reach the fans, and the tech that connects them, so <span className="text-accent">each part makes the others stronger.</span></blockquote></div></section>; }
