import Image from 'next/image';
import { SectionHeading } from './ui/SectionHeading';

const cards = [{ src: '/assets/firecrackers.png', alt: 'Firecrackers FC squad in Talent 50 kit', badge: 'Operational', title: 'Firecrackers FC', copy: "A grassroots club in Nigeria, founded and operational since 2024, with an active youth scouting and development pipeline. It's our live proof-of-concept and first source of talent." }, { src: '/assets/boardgame.png', alt: 'A packed arena watching a giant draughts board game final', badge: 'New format', title: 'Digital Board Game League', copy: "A fresh take on a classic board game, reimagined for digital, competitive play across Africa. We're keeping the specifics under wraps for now." }];

export default function Traction() { return <section id="traction" className="bg-paper py-[clamp(84px,13vh,170px)] text-ink"><div className="mx-auto max-w-content px-5 md:px-[5vw] lg:px-[72px]"><SectionHeading eyebrow="06 — Where we are" title="Built on the ground. Not on paper." theme="light" titleClassName="max-w-[22ch]" /><div className="grid gap-[clamp(28px,3.5vw,56px)] md:grid-cols-2">{cards.map((card) => <article key={card.title}><div className="relative aspect-[16/10] overflow-hidden rounded-4xl shadow-soft-lg"><Image src={card.src} alt={card.alt} fill sizes="(max-width: 768px) 100vw, 50vw" className="object-cover object-[50%_35%] grayscale-[.1] transition-transform duration-300 hover:scale-[1.02]" /><span className="absolute left-4 top-4 rounded-full bg-ink px-3 py-1.5 font-mono text-[11px] uppercase tracking-[0.14em] text-text-light">{card.badge}</span></div><div className="pt-[clamp(22px,2.2vw,32px)]"><h3 className="mb-1 font-display text-[21px]">{card.title}</h3><p className="text-[14.5px] font-light leading-[1.55] text-muted-dark">{card.copy}</p></div></article>)}</div><div className="mt-[22px] flex items-center gap-2 text-xs tracking-[0.02em] text-ink/60"><span className="inline-block h-px w-[18px] bg-ink/40" />&amp; others in build. More to share soon.</div></div></section>; }
